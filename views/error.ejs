<%- include('partials/header') %>
<div class="<%= user ? '' : 'panel card-auth' %>">
  <div class="page-head"><h1><%= title %></h1></div>
  <div class="panel"><p><%= message %></p><a class="btn" href="<%= user ? '/dashboard' : '/login' %>"><%= user ? 'Back to dashboard' : 'Go to sign in' %></a></div>
</div>
<%- include('partials/footer') %>
